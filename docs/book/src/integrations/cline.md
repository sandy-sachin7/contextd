# Cline / Roo Code (VSCode)

1. Open the MCP Servers settings (via the "MCP Servers" tab or command palette).
2. Add a new server:
   - **Name**: `contextd`
   - **Command**: `/path/to/contextd/target/release/contextd`
   - **Args**: `mcp`, `--config`, `/path/to/contextd.toml`
